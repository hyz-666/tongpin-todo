[workspace]
resolver = "3"
members = [
    "crates/todo-domain",
    "crates/todo-storage",
    "crates/todo-crypto",
    "crates/todo-protocol",
    "crates/todo-discovery",
    "crates/todo-core",
    "crates/todo-uniffi",
    "crates/todo-testkit",
]

[workspace.package]
version = "0.1.0"
edition = "2024"
rust-version = "1.98"

[workspace.dependencies]
# Exact-compatible dependency ranges are added here as later tasks introduce them.
